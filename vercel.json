{
  "installCommand": "cd \"Novaryn Hub\" && bun install",
  "buildCommand": "cd \"Novaryn Hub\" && bun run build && cd .. && rm -rf dist && cp -R \"Novaryn Hub/dist\" dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!assets/|favicon.ico|vite.svg).*)", "destination": "/index.html" }
  ]
}
