{
  "buildCommand": "cd 'Novaryn Hub' && bun install && bun run build",
  "outputDirectory": "Novaryn Hub/dist",
  "rewrites": [
    { "source": "/((?!assets/|favicon.ico|vite.svg).*)", "destination": "/index.html" }
  ]
}
