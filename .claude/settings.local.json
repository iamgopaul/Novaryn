{
  "permissions": {
    "allow": [
      "Bash(git add scripts/bundle-api.ts)",
      "Bash(git commit -m \"fix: remove invalid bundle:true from Bun.build config:*)",
      "Bash(git push)",
      "Bash(git commit -m \"fix: bundle src/ entry points as ESM to match api/ import format:*)"
    ]
  }
}
